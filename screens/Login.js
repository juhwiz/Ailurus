import { Background, Title, LinkText } from '../styled';

export default function TelaLogin(){
    return(
        <Background>
            <Title>Tela Login</Title>
        </Background>
    );

}