import { Background, Title, LinkText } from '../styled';

export default function TelaPerfil(){
    return(
        <Background>
            <Title>Esta e a tela de perfil</Title>
        </Background>
    );
}